import { strict as assert } from 'assert';
import { AI } from './AI';


describe('AI', () => {
    describe('constructor()', () => {
        it('should create an instance of AI', () => {
            const ai = new AI();
            assert.notEqual(ai, undefined);
        });
    });
});